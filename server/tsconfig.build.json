// What `pnpm build` uses. Same compiler options as tsconfig.json, but narrowed
// to src/ and switched back on for emit.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "noEmit": false
  },
  "include": ["src/**/*"]
}
